[project]
name = "day17-implementation-lang"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.2.12",
]

[tool.uv.workspace]
members = [
    "venv",
]
